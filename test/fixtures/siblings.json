{
  "note": "Pinned revisions of the sibling repositories sb3-creator's cross-repo gates consume. CI checks these exact commits out beside the repo (.github/workflows/ci.yml) so the gates RUN rather than skip; test/helpers/siblings.mjs turns an absent sibling into a failure when CI=true. Update a pin deliberately, in its own commit, with the suite green against it. `rev` MUST be the FULL 40-character SHA: actions/checkout only recognises a full SHA as a commit, and turns anything shorter into `git fetch origin +refs/heads/<ref>*` \u2014 a branch glob that matches nothing. An abbreviated pin here took every CI run on main down at the checkout step, before lint, tests, the mutation prover or the build ran at all. gate-integrity asserts the length; `revShort` exists only for prose.",
  "siblings": {
    "bw-board": {
      "repo": "CrispStrobe/bw-board",
      "rev": "a301937d9912a66aa4a7cdae403e39a337ac9eb2",
      "marker": "src/board.js",
      "licence": "MIT",
      "why": "the device/board simulator the gallery e2e and physics gates drive. Advanced from 88e9668 for the source-and-transistor-current honesty run (20283ab, b5c02b1, a301937): vsource honours rInternal in DC and swept AC, a saturated PNP stamps its E-B junction again, FET extraction reads the region map, and a buzzer branch current is extractable. That closes BOTH declines this repo's EXPECTED.md gate had recorded against the engine \u2014 the rInternal canary and the transistor-terminal-current decline \u2014 Measured either side of the bump alone, with nothing else changed: the corpus moves 2 claims from declined to CHECKED (47-battery-led's 3.5 mV internal drop, pc32-pnp-high-side's 0.43 mA base), both AGREEING, and nothing regresses \u2014 1209 compared of 2358 to 1211. test/device-branch-agreement.test.mjs is red on 88e9668, 20283ab AND b5c02b1 and green only here, so the suite genuinely requires this revision rather than merely tolerating it.",
      "pinnedOn": "2026-08-24",
      "revShort": "a301937"
    },
    "bw-circuit-ui": {
      "repo": "CrispStrobe/bw-circuit-ui",
      "rev": "410f8cecdbefec6eb8a2031e193e26ddf088ac3f",
      "marker": "src/model/circuit.js",
      "licence": "MPL-2.0",
      "why": "the canonical circuit model, engine, and the ONE wire-endpoint dialect reader (src/model/wire-endpoints.js) the corpus gates read circuit.json through",
      "pinnedOn": "2026-08-23",
      "revShort": "410f8ce"
    }
  }
}
