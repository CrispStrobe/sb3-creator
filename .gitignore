# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Browser-harness build artifacts (regenerated by `npm run test:browser`)
test/browser/harness.bundle.js
test/browser/shots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent context: local paths and operational detail, not for the repo.
CLAUDE.md
corpus/
test/fixtures/6502-oled-counter.o

# Where CI checks out bw-board and bw-circuit-ui so the cross-repo gates run
# rather than skip (.github/workflows/ci.yml), and where a developer reproducing
# a CI failure locally will put them. Two whole sibling repositories; never ours
# to commit. eslint.config.js ignores the same path for the same reason.
siblings/
