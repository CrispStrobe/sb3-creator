# disp-mono-lcd — expected behaviour

SSD1306 as monochrome graphic LCD. Same I2C protocol as OLED.

```assert
display: mono_lcd (ssd1306)
interface: i2c
text_line_0: MONO LCD
counter_row: 4
refresh_ms: 1000
```
