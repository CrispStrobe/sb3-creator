# Op-amp threshold

The two divider networks provide the non-inverting and inverting inputs. As the
potentiometer moves through the reference, the output changes polarity and the
indicator responds.

```assert
# Opamp comparator: reference = VCC/2 = 2.5V
net src.pos V 5.00 +-0.01
```
