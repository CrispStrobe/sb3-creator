# Motor clamp

The motor starts when the switch closes. When it opens, the flyback diode is
reverse-biased during normal operation but becomes the current path as the
motor winding collapses, reducing the voltage spike at the switch.
