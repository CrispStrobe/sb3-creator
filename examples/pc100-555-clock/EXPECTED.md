# Expected behaviour — The clock — a machine needs a heartbeat

| setting | effect |
|---|---|
| ~1 Hz | LED blinks |
| bigger C | slower |
| smaller C | faster |

Asserted by simulation in bw-circuit-ui's test/logic-ladder.test.js (the same circuit, wire for wire) and re-checked seated in test/logic-examples.test.js.

Parts on a board: 7. Off board (rails and parts with no breadboard footprint): 2.
