DEVICE ARDUINO-UNO
CLOCK 16000000
PIN button1 = D2 INPUT
PIN led1 = D13 OUTPUT

# Light the LED while the button is pressed
WHEN flag clicked:
  FOREVER:
    IF read button1 THEN:
      turn on led1
    ELSE:
      turn off led1
    wait 0.05 seconds
