DEVICE STC12C5A60S2
CLOCK 11059200
PIN potA = P1.6 ANALOG
PIN potB = P1.7 ANALOG
PIN led1 = P1.0 OUTPUT ACTIVE LOW

# Compare two potentiometers; LED on if A > B
WHEN flag clicked:
  FOREVER:
    IF read potA > read potB THEN:
      turn on led1
    ELSE:
      turn off led1
    wait 0.1 seconds
