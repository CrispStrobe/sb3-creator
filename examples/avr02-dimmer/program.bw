DEVICE ARDUINO-UNO
CLOCK 16000000
PIN pot1 = A0 ANALOG
PIN led1 = D9 OUTPUT

# Read the pot and set the LED brightness
WHEN flag clicked:
  FOREVER:
    set led1 brightness to (read pot1 / 1023 * 100)
    wait 0.05 seconds
