# Selectable reference

The resistor chain creates predictable intermediate voltages. Closing the
switch connects the measurement load to the upper tap; opening it leaves the
divider unloaded, demonstrating that a load can change a measured reference.
