# Ported from arduino-examples/examples/04.Communication/Dimmer/Dimmer.ino, CC0
# Adapted: pot on A0 replaces serial input (no serial input in blocks)
DEVICE ARDUINO-UNO
CLOCK 16000000
PIN pot = A0 ANALOG
PIN led = D9 PWM

SPRITE Cat:
  WHEN flag clicked:
    forever:
      set brightness to ((read pot * 100) / 1023)
      set led to brightness percent
      wait 0.02 seconds
